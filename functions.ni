
// Function that shows return value type and the way to define functions.
def int Square(int x)
{
	return x * x;
}

// Function that shows explicit pass by reference.
def void Reference(string& name)
{
	name += "Lol";
}
